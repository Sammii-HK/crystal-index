// import { NewLocationForm } from '../../components/Organisms';
import { createLocation } from '../../lib/helpers/locationRequests';

const CreateLocations: React.FC = () => {
  return (
    <div className="my-2">
      {/* <NewLocationForm onCreateLocation={createLocation} /> */}
    </div>
  )
}

export default CreateLocations

