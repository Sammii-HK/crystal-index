version: "3.9"
services:
  crystalsfrontend:
    container_name: crystalsfrontend
    build:
      context: .
      dockerfile: Dockerfile.frontend
    depends_on:
      - crystalsbackend
    ports:
      - "80:8080"
  crystalsbackend:
    container_name: crystalsbackend
    build:
      context: .
      dockerfile: Dockerfile.backend
    depends_on:
      - crystalsdb
    ports:
      - "3000:3000"
    environment:
      DB_HOST: crystalsdb
      DB_USERNAME: hellosammii
      DB_PASSWORD: hellocrystals
    command: bash -c "npx wait-port crystalsdb:5432; npx sequelize-cli db:migrate; node server.js"
  crystalsdb:
    container_name: crystalsdb
    image: postgres
    environment:
      POSTGRES_USER: pi
      POSTGRES_PASSWORD: 
      POSTGRES_DB: crystal_index_dev
